{
  "name": "zero-design-system-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to sync Zero design system tokens",
  "main": "code.js",
  "scripts": {
    "build": "tsc",
    "build:sync": "tsc --project tsconfig-sync.json",
    "build:all": "tsc && tsc --project tsconfig-sync.json",
    "dev": "tsc --watch"
  },
  "author": "Zero Team",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "typescript": "^5.3.0"
  }
}
